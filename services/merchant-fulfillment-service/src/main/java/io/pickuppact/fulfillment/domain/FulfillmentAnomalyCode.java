package io.pickuppact.fulfillment.domain;

public enum FulfillmentAnomalyCode {
    STARTED_LATE,
    READY_TOO_EARLY,
    READY_LATE,
    CANCEL_AFTER_PREPARATION,
    RESCHEDULE_AFTER_PREPARATION
}
