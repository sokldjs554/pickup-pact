# 자동 복구 공개 앱과 영상의 검증 기록

## 앱 기준

- 검증한 앱 main: `000a106cc5fda6a3be595a83f938087096c3ad63`
- 앱 tree: `39399c862cd3ce6980672b2a5bb6f82864ef5499`
- PR #30의 자동 복구·HTTP 매장 구성과 PR #31의 공개 비교 화면 수정이 반영됐다.
- 해당 SHA의 main workflow 9개 성공, Render `dep-dar9pgm7bikc73b2aoc0`의 commit 일치·live 확인.
- 공개 제품은 한 호스트에서 별도 매장 HTTP 프로세스와 독립 SQLite 파일을 사용한다. 실가맹점·실제 결제·멀티 호스트 운영은 아니다.

## 원본 실행 증거

`route-product` main 실행: https://github.com/sokldjs554/pickup-pact/actions/runs/36158962973

| 산출물 | ID | ZIP SHA-256 |
|---|---|---|
| main CI | 10874473483 | `3e9f7cc6d455dede81105b2fbc164d3d7ced917cecbcbf9093cff6fdaa92062f` |
| 동일 SHA 공개 검증 | 10875033754 | `4f12138dd43fbf728caaf6761327916dcecd773884d5396094018c8f56b5b0a1` |
| 공개 원본 촬영 | 10875566186 | `4653253633ba9b21132e702c3bd888a61d839ca6e3610826235c8f511f775108` |
| 장면 시점 검토 후 편집 | 10876436456 | `f9d73eb4aeaefcf372140175ae46c237a693ac1c8764543411f4817bd130aa80` |

CI의 tested-commit과 소스 280개 파일을 검토본과 대조했다. Python은 CI 332개 × 3회, 로컬은 Flask 제외 329개 × 3회다. 합산하지 않는다. 공개 브라우저는 복구·주문·혜택·문구 각각 데스크톱/모바일 × 3회, 총 24회이며 해당 앱 SHA·성공·페이지 오류 0을 확인했다.

기존 JVM·release-gate 반복 1·2·3차, 전체 Docker 구성 2회, 보존 데이터 경계·SQL 실행계획·Terraform은 별도 검증이다. 로컬에 없는 도구나 정책상 차단된 브라우저를 로컬 성공으로 세지 않았다.

## 공개 실패를 추적하고 수정한 과정

이전 앱 `414dac8…`의 공개 실행 `36131639244`는 마지막 비교 결과 6행을 5초 안에 보지 못해 실패했다. 앞의 거절·자동 복구·수령이 성공했더라도 전체 공개 성공으로 보고하지 않았다.

진단 실행 `36156894258`의 첫 시도는 초기 health 20초 대기에서 끝났다. 재시도에서 앱을 바꾸지 않고 관찰한 실제 비교 응답은 데스크톱 7.807초·모바일 7.714초, HTTP 200이었다. 그동안 결과는 비어 있고 전체 주문 조작이 잠겼다.

PR #31에서 비교의 진행 안내·입력 조건 고정·중복 방지·실패 안내를 넣고 주문 조작과 분리했다. 검사는 요청 전송 5초, 응답 30초, 기존 DOM 표시 5초를 구분하되 HTTP 200·6개 결과·원래 금액·주문·혜택 단언은 유지했다. 새 공개 반복의 비교 응답은 8.720~9.598초였다. 처리량이나 응답 속도가 개선됐다고 주장하지 않는다.

## 실제 촬영과 편집

촬영 실행: https://github.com/sokldjs554/pickup-pact/actions/runs/36161012075

처음 촬영은 모바일에서 이전 명령의 응답이 컨트롤을 다시 그리는 동안 선택한 실패 조건이 초기화될 수 있어 중단됐다. 앱 동작이나 복구 시간을 바꾸지 않고, 각 명령 완료와 실제 서버의 선택된 실패 조건·PENDING 응답을 확인하도록 촬영 절차를 수정했다. 성공한 촬영에는 데스크톱 수동 recover POST가 없으며, 거절 전후 원래 주문·혜택이 같다.

전 매장 500원 쿠폰과 1,000P를 적용한 같은 주문이 최종 3,200원 모의 결제 1건, 1,000P 사용·32P 적립·잔액 1,032P로 끝났다. 촬영 전후 health는 앱 SHA와 같으며 페이지 오류는 0이다.

원본 WebM SHA-256: `3db869173b8f74f5a42d9057e1b3d5f6c7a493db0ec24d4e40a0f8afd121e455`.

첫 편집의 벽시계 장면 시각과 영상 첫 프레임 기준이 맞지 않아 원본을 시각적으로 검토한 뒤 실제 영상 PTS의 구간으로 다시 잘랐다. `scripts/edit_recovery_recording.py`는 이 원본 해시가 맞아야 실행된다. 재생 속도는 1.0이고 자막은 앱 화면 바깥에 둔다. 가상 제조 조작·장면 사이 대기를 생략했지만 서버 결과나 앱 내용을 합성하지 않았다. 편집에는 재인코딩이 포함되므로 원본과 픽셀이 비트 단위로 같다는 주장은 아니다.

최종 편집 실행: https://github.com/sokldjs554/pickup-pact/actions/runs/36162048878

- 최종 길이 **39.72초**, MP4/GIF 전체 디코딩 확인.
- MP4 SHA-256 `e3aef8feb7cfbdaff7c7d97180c42353c253e030624c5b63dee73f06ef4c502a`
- GIF SHA-256 `00ab55d6133e81f55213634fc838c8e9e250719228fb420d5016b1eedd9b40f7`
- 실제 공개 화면의 거절·확인 중·복구·영수증·비교와 자막을 대조했다. 이전 추가금 중심 영상은 현재 README에서 교체했다.

## 이후 미디어 커밋의 의미

이 문서와 영상 반영은 앱 코드 변경이 아니다. 최종 PR에서는 README·촬영/편집 스크립트·미디어·공고 설명만 바뀌는지 확인한다. 최종 main의 CI·공개 배포는 그 SHA로 다시 검사하고 PR 기록에 남긴다. 이 문서의 앱 검증 결과를 아직 실행하지 않은 미래 main의 성공으로 대신하지 않는다. 지원용 자소서·포트폴리오는 공개 저장소에 넣지 않는다.
